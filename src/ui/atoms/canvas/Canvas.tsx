import styled from 'styled-components';

type Props = {
};

const Canvas = styled.canvas<Props>`
  border-radius: 0.25rem;
  box-shadow: 0.06rem 0.06rem 0.12rem 0.12rem rgba(0, 0, 0, 0.25);
`;

export default Canvas;
